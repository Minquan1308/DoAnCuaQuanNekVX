@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<TH_Lap3.Models.Category>

@* <div class="dropdown">
    <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        Categories
    </button>
    <ul class="dropdown-menu">

        @foreach (var category in Model)
        {
            <li><a class="dropdown-item" href="#">@category.Name</a></li>
        }

    </ul>
</div> *@
<div class="dropdown">
    <button class="btn btn-secondaryx btn-outline-light bg-blue dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        Danh mục
    </button>
    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        @foreach (var category in Model)
        {
            <li><a class="dropdown-item" href="@Url.Action("FilterByCategory", "Product", new { categoryId = category.Id })">@category.Name</a></li>
        }
    </ul>
</div>
